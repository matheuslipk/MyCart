import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fffa',
    paddingHorizontal: 5,
  },
  input: {
    paddingHorizontal: 5,
    paddingVertical: 10,
    marginTop: 10,
    borderRadius: 5,
    backgroundColor: '#fff',
    fontSize: 16,
  },
  btnAdd: {
    marginTop: 10,
    borderRadius: 5,
  },
  textBtn: {
    textAlign: 'center',
  },
});
