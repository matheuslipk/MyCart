import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    marginTop: 10,
  },
  textLabel: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  input: {
    paddingHorizontal: 5,
    paddingVertical: 10,
    borderRadius: 5,
    backgroundColor: '#fff',
    fontSize: 16,
  },
});
